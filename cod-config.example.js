/**
 * Copy to cod-config.js and paste your COD Network seller API token.
 * Do not commit cod-config.js if it contains a real token (see .gitignore).
 */
window.COD_CONFIG = {
    token: 'PASTE_YOUR_JWT_HERE',
    debug: true
};
